{
  "area": "features-mockup",
  "currentState": "The collab-web mockup ships a respectable but narrow chat surface: a three-tab LeftSidebar (Controls / Sessions / Sources), a SessionList wired to bridge endpoints for create/rename/delete/start/stop/state, a UserControlsPanel exposing model, mode (normal/safe/debug), extended-thinking toggle, and a DataSourcesPanel that only renders a static refresh button. The Composer supports send / abort / regenerate but has no slash-command grammar, no plan-mode toggle, no advisor pair, no checkpoint/rewind, and no MCP/skill registry. Tool-rendering is the most complete area — 32 omp tools (bash, edit/apply_patch, read, write, find, search/grep, search_bm25, ast_grep/ast_edit, fetch, web_search, browser/puppeteer, generate_image, inspect_image, eval/js/python/notebook, lsp, debug, github, irc, ssh, ask, goal, todo, task, job/await/poll/cancel_job, resolve, recall/reflect/retain, report_finding/report_tool_issue, yield) are mapped in `tool-render/registry.ts` with renderer files in `tool-render/tools/`. The bridge has zero endpoints for MCP, skills, advisor, hindsight memory store, checkpoints, kernel lifecycle, or live-collab session sharing — `/api/v1/chat/*` covers only sessions, data-sources, and runtime-config (with a hardcoded 7-entry `AVAILABLE_MODELS` list rather than reading omp's `/model` output).",
  "keyFiles": [
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\components\\LeftSidebar.tsx",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\components\\UserControlsPanel.tsx",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\components\\DataSourcesPanel.tsx",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\components\\SessionList.tsx",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\components\\ChatComposer.tsx",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\pages\\MainChatPage.tsx",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\types\\chat.ts",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\features\\chat\\api\\chatApi.ts",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\tool-render\\registry.ts",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\tool-render\\tools\\",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\lib\\chat-client.ts",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\collab-web\\src\\lib\\rpc-client.ts",
    "C:\\Users\\Mark-MJ\\Documents\\oh-my-pi\\packages\\desktop-bridge\\src\\routes\\chat.ts"
  ],
  "gaps": [
    {
      "id": "feat-session-mgmt",
      "severity": "low",
      "description": "Session list & management — STATUS: done. SessionList component plus bridge endpoints GET/POST /api/v1/chat/sessions, DELETE :id, POST :id/start|stop|rename, GET :id/state cover create/rename/delete/switch/start/stop. Persisted via makeStore('sessions'). Minor: no per-session metadata (cwd, model override, branch). Evidence: chat.ts:60-134, SessionList.tsx, useChatStateMachine.ts."
    },
    {
      "id": "feat-model-picker",
      "severity": "high",
      "description": "LLM provider/model picker — STATUS: partial. UserControlsPanel renders a <select> over availableModels and POSTs /runtime-config; MainChatPage:171-179 splits provider/model on '/' and calls client.sendSetModel. PROBLEM: AVAILABLE_MODELS in chat.ts:33-41 is a hardcoded list (claude-opus-4-8, sonnet-4-6, gpt-5.5, gemini-3-flash, grok-4-fast). Bridge does not call omp's `/model` to discover real providers/models, doesn't expose API-key status, and has no provider-config (base URL, key path) surface. Evidence: chat.ts:33-41, UserControlsPanel.tsx:29-46."
    },
    {
      "id": "feat-mcp-registry",
      "severity": "high",
      "description": "MCP server list / add / remove / status — STATUS: missing. Zero `mcp` references in collab-web/src or desktop-bridge/src (Grep). No UI panel, no bridge endpoint, no store file, no tool-renderer for MCP-status. omp speaks MCP but the mockup ignores it entirely."
    },
    {
      "id": "feat-skill-registry",
      "severity": "high",
      "description": "Skill registry (.omp/agent, inherited .claude/.cursor rules) — STATUS: missing. No `skill` references in chat features or bridge routes. No UI to list/enable/disable skills, no inheritance preview, no editor for project rule files. Important because omp's selling point vs Claude Code is the inherited skill graph."
    },
    {
      "id": "feat-tool-views",
      "severity": "low",
      "description": "Tool views (32 omp tools) — STATUS: done. tool-render/registry.ts maps all 32 wire tool names plus aliases (puppeteer→browser, apply_patch→edit, grep→search, js/python/notebook→eval, await/poll/cancel_job→job) to dedicated renderers. genericRenderer fallback for unknown tools. Evidence: registry.ts:1-87, tool-render/tools/ has 32 .tsx files."
    },
    {
      "id": "feat-slash-commands",
      "severity": "blocker",
      "description": "Slash commands (/model, /collab, /review, /commit, /advisor, /debug, /reload-plugins, /init, /verify, ...) — STATUS: missing. ChatComposer.tsx:43-55 sends raw text via client.sendPrompt with no slash detection, autocomplete, parameter prompting, or command palette. No slashCommand registry in collab-web/src. The only '/' references are model-id-splitting and JSX route paths. omp/Claude Code's slash-command discovery (skill list, MCP tool list) has no surface here."
    },
    {
      "id": "feat-plan-mode",
      "severity": "high",
      "description": "Plan mode toggle — STATUS: missing. RuntimeConfig.mode is {normal,safe,debug} — there is no `plan` value, no plan-approval UI, no plan-diff viewer. Composer doesn't route plan-mode prompts differently. Grep for 'plan' returns only AVAILABLE_MODELS strings."
    },
    {
      "id": "feat-advisor",
      "severity": "high",
      "description": "Advisor pair (second model watching) — STATUS: missing. No `advisor` references in collab-web/src or desktop-bridge/src. No second-model configuration in RuntimeConfig, no transcript lane for advisor commentary, no bridge wiring to omp's advisor coprocess."
    },
    {
      "id": "feat-hindsight",
      "severity": "high",
      "description": "Hindsight (retain/recall/reflect) — STATUS: partial (tools only). retain/recall/reflect tool renderers exist (memory-retain.tsx, memory-recall.tsx, memory-reflect.tsx) so call sites are visualized, but there is NO sidebar memory browser, no project-scoped memory store surface, no manual retain UI, no recall-search panel. The skill is invocable but not curatable."
    },
    {
      "id": "feat-checkpoint-rewind",
      "severity": "high",
      "description": "Checkpoint / rewind — STATUS: missing. No checkpoint, rewind, snapshot, or fork references in chat features or bridge. ChatComposer's regenerate maps to plain abort+regenerate (rpc-client.ts:145-147) with no turn-history rewind. omp's checkpoint store has no UI."
    },
    {
      "id": "feat-kernels",
      "severity": "medium",
      "description": "Code execution kernels (Python+Bun) — STATUS: partial (tool render only). eval.tsx (lines 1-3 'persistent kernel py/js/rb/jl') renders kernel call cards, registry aliases js/python/notebook→eval. No kernel-lifecycle UI (start/stop/restart, list active kernels, install deps), no bridge endpoint to manage kernels. Users see what ran but can't manage runtimes."
    },
    {
      "id": "feat-lsp-debug",
      "severity": "low",
      "description": "LSP / Debug panes — STATUS: partial. Tool renderers lsp.tsx and debug.tsx exist and render call results inline; no dedicated dock panes. Acceptable per prompt note ('or just rely on tool view cards'), but no surface for LSP server list or debug-session attach controls."
    },
    {
      "id": "feat-collab-live",
      "severity": "medium",
      "description": "/collab live session sharing — STATUS: partial. Sessions get a relay link (chat.ts:47-51 newLink() builds ws://127.0.0.1:relayPort/r/{roomId}.{key}) and GuestClient connects to it, but there is NO UI to invite a peer, no read-only viewer mode toggle, no participant list, no shareable URL displayed to the user, no /collab slash command. The plumbing exists; the social/share UI does not."
    },
    {
      "id": "feat-image-gen-browser-web",
      "severity": "low",
      "description": "Image gen / browser / web_search inline results — STATUS: done at render level. generate-image.tsx, browser.tsx (also aliased from puppeteer), web-search.tsx, inspect-image.tsx, fetch.tsx renderers are all in tool-render/tools/. Tool results render inline. No higher-level surfaces (image gallery, web-clipping inbox, browser sidecar window) but per prompt's scope this is acceptable."
    },
    {
      "id": "feat-settings",
      "severity": "high",
      "description": "Global settings surface — STATUS: stub. UserControlsPanel only exposes model/mode/thinking. No general settings page for: API keys, default cwd, default skill set, allowed/blocked tools, hook config, telemetry, theme, font, keybindings. settings.json discovery + edit surface is absent."
    },
    {
      "id": "feat-data-sources",
      "severity": "medium",
      "description": "Data sources panel — STATUS: stub. DataSourcesPanel only renders an empty list (default sources=[]) with a single refresh button per row (chat.ts:140-148 always sets status='connected'). No add-source flow, no source types (file folder, web RSS, DB, repo, RAG index), no inline indexing progress. Designed as a placeholder."
    }
  ],
  "proposedActions": [
    {
      "id": "p4-model-discovery",
      "phase": "p4-features",
      "description": "Replace hardcoded AVAILABLE_MODELS in routes/chat.ts with a bridge call that runs `omp /model --json` (or reads omp's model manifest) at start-up and on POST /runtime-config/refresh. Surface provider grouping in UserControlsPanel (optgroup per provider) and expose API-key presence so missing keys show a warning chip."
    },
    {
      "id": "p4-mcp-registry",
      "phase": "p4-features",
      "description": "Add MCP surface: bridge routes GET/POST/DELETE /api/v1/mcp/servers backed by state/mcp.json, plus GET :id/status that proxies to omp's MCP probe. New LeftSidebar tab 'MCP' with add-server dialog (name, command, args, env). Stream server state via WS so red/green dots update live."
    },
    {
      "id": "p4-skill-registry",
      "phase": "p4-features",
      "description": "Add Skills surface: bridge route GET /api/v1/skills that scans cwd for .omp/agent, .claude/skills, .cursor/rules and returns merged list with inheritance source. UI panel under new 'Skills' tab; checkbox to enable/disable per session, link to open underlying file."
    },
    {
      "id": "p4-slash-palette",
      "phase": "p4-features",
      "description": "In ChatComposer, detect leading '/' to open a command palette popover. Source commands from skill registry + built-ins (/model, /collab, /review, /commit, /advisor, /debug, /reload-plugins, /init, /verify). Tab-completes args; on submit, route to client.sendSlashCommand (add to ChatClient interface) which RPC-forwards to omp; legacy fallback prepends '/' into sendPrompt."
    },
    {
      "id": "p4-plan-mode",
      "phase": "p4-features",
      "description": "Extend RuntimeConfig.mode union with 'plan'. Add toggle in UserControlsPanel; when plan, Composer shows a Plan badge and the Transcript renders an additional Plan card (new tool renderer 'plan'). Approve/edit/reject buttons map to client.sendSlashCommand('/approve' | '/revise')."
    },
    {
      "id": "p4-advisor",
      "phase": "p4-features",
      "description": "Add advisorModel field to RuntimeConfig plus a Composer-pinned secondary lane (`<TranscriptLane role='advisor'>`) that subscribes to a `advisor.delta` WS event from omp. Bridge spawns/manages the second omp process (or talks to omp's built-in advisor coproc) via state/advisor.json."
    },
    {
      "id": "p4-hindsight-browser",
      "phase": "p4-features",
      "description": "New 'Memory' tab in LeftSidebar listing retained items (search box, tag chips, source link). Bridge routes GET /api/v1/memory/items, POST /retain (manual), POST /forget. Reuse memory-retain/recall/reflect renderers for detail view."
    },
    {
      "id": "p4-checkpoint-rewind",
      "phase": "p4-features",
      "description": "Bridge routes GET /api/v1/chat/sessions/:id/checkpoints + POST .../rewind?to=<cp-id> that drive omp's checkpoint store. In Transcript, render a left-margin rail with checkpoint dots per turn; click → 'Rewind to here' confirmation. Replace regenerate's silent abort with an explicit rewind-1 if the user holds shift."
    },
    {
      "id": "p4-kernel-manager",
      "phase": "p4-features",
      "description": "Add a 'Kernels' subpanel under Controls listing active py/bun kernels (GET /api/v1/kernels), with restart and 'install package' buttons. Bridge proxies to omp's kernel manager. eval.tsx tool cards link to their kernel row."
    },
    {
      "id": "p4-collab-share",
      "phase": "p4-features",
      "description": "Show the relay link from chat.ts:newLink() as a 'Share' button in the chat header that copies a deeplink and opens a peer dialog (QR + read-only checkbox). Add participant chip row driven by GuestClient peer-list events. Plumb a /collab slash command that toggles guest-write permission."
    },
    {
      "id": "p4-settings-page",
      "phase": "p4-features",
      "description": "Add /settings route with sections: API Providers (key paths, base URLs), Defaults (cwd, model, mode, thinking), Tools (allowed/blocked toolset per skill), Hooks (preview only — write via /update-config), Appearance (theme, font, density), Keybindings. Bridge: GET/PATCH /api/v1/settings backed by ~/.omp/settings.json with schema validation."
    },
    {
      "id": "p4-data-sources-real",
      "phase": "p4-features",
      "description": "Promote DataSourcesPanel from stub: source types (folder, git repo, web URL, sqlite/duckdb, MCP-resource). Add-source dialog with type-specific fields. Bridge wires to omp's indexer; report progress percentage via WS event so the dot shows loading→connected transitions."
    },
    {
      "id": "p4-image-gallery",
      "phase": "p4-features",
      "description": "Optional polish: a 'Gallery' tab aggregating all generate_image and inspect_image outputs from the active session for quick reuse — backed by an in-memory index built from the existing tool-call stream (no new bridge route needed). Lowest priority."
    }
  ]
}